// cauce-core — domain model, agent runtime, conversation state, and the plugin SPI.
//
// This is the hexagonal domain core: it defines ports (interfaces) and stays free of
// HTTP, PostgreSQL, channels, and LLM providers. Build configuration is inherited from
// the root project (see ../build.gradle.kts). Add module dependencies below as it grows.
