// cauce-enterprise — commercial modules.
//
// ┌────────────────────────────────────────────────────────────────────────────┐
// │ LICENSE: This module is NOT under the Business Source License 1.1 that      │
// │ covers the rest of Cauce. It is governed by a SEPARATE COMMERCIAL LICENSE.  │
// │                                                                            │
// │ Code in cauce-enterprise MAY depend on BUSL-licensed modules.              │
// │ Code in BUSL-licensed modules MUST NEVER depend on cauce-enterprise.       │
// └────────────────────────────────────────────────────────────────────────────┘
//
// Build configuration is inherited from the root project (see ../build.gradle.kts).
// No dependencies and no classes yet — kept as an isolated, unreferenced module.
