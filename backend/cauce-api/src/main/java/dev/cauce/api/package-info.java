/**
 * Cauce API — REST API surface and the Spring Boot application module.
 *
 * <p>The aggregating application module: it wires together the BUSL-licensed backend
 * modules and exposes the REST API. It does not depend on {@code cauce-enterprise}.
 */
package dev.cauce.api;
