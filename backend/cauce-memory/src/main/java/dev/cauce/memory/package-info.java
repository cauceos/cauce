/**
 * Cauce memory — persistent state, cross-channel identity, and vector retrieval.
 *
 * <p>Holds long-lived conversational and identity state and provides vector-based
 * retrieval (pgvector). All access is tenant-scoped.
 */
package dev.cauce.memory;
