# Integration-test configuration. The datasource is provided by Testcontainers
# via @ServiceConnection, so no datasource URL is set here.

# Flyway owns the schema; Hibernate only validates the entity mapping against it.
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
